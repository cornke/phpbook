### Оформление вывода из базы данных
//1
```php
<meta charset="utf-8">
<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');

$query = "SELECT * FROM users";
$data = mysqli_query($link, $query) or die(mysqli_error($link));
$res = '';

foreach ($data as $elem) {
$res .= '<div>';
$res .= '<h2>' . $elem['name'] . '</h2>';
$res .= '<p>' . $elem['age'] . ', ' . '<b>' . $elem['salary'] . '</b>' . '</p>';
$res.= '</div>';
}

echo $res;
```

//2
```php
<meta charset="utf-8">
<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');

$query = "SELECT * FROM users";
$data = mysqli_query($link, $query) or die(mysqli_error($link));
$res = '<table>';
$res .= '<th>' . 'id' . '</th>';
$res .= '<th>' . 'name' . '</th>';
$res .= '<th>' . 'age' . '</th>';
$res .= '<th>' . 'salary' . '</th>';
foreach ($data as $elem) {
    $res .= '<tr>';
    $res .= '<td>' . $elem['id'] . '</td>';
    $res .= '<td>' . $elem['name'] . '</td>';
    $res .= '<td>' . $elem['age'] . '</td>';
    $res .= '<td>' . $elem['salary'] . '</td>';
    $res.= '</tr>';
}
$res .= '</table>';

echo $res;
```

//3
```php
<meta charset="utf-8">
<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');

$query = "SELECT * FROM users";
$data = mysqli_query($link, $query) or die(mysqli_error($link));
$res = '<ul>';

foreach ($data as $elem) {
    $res .= '<td>' . $elem['name'] . '</td>';
}
$res .= '</ul>';

echo $res;
```

### Удаление данных из БД с помощью GET запросов
//1
```php
<meta charset="utf-8">
<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');

$del = $_GET['user_id'];
$query = "DELETE FROM users WHERE id=$del";
$res = mysqli_query($link, $query) or die(mysqli_error($link));
echo $res;
```

//2
```php
<meta charset="utf-8">
<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');

$del = $_GET['del'];
$query = "DELETE FROM users WHERE id=$del";
$res = mysqli_query($link, $query) or die(mysqli_error($link));

$query = "SELECT * FROM users";
$data = mysqli_query($link, $query) or die(mysqli_error($link));
$res = '';

foreach ($data as $elem) {
    $res .= '<a href="?del=' . $elem['id'] . '">' . $elem['name'] . '</a>';
}

echo $res;
```

//3
```php
<meta charset="utf-8">
<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');

$del = $_GET['del'];
$query = "DELETE FROM users WHERE id=$del";
$res = mysqli_query($link, $query) or die(mysqli_error($link));

$query = "SELECT * FROM users";
$data = mysqli_query($link, $query) or die(mysqli_error($link));
$res = '';

foreach ($data as $elem) {
    $res .= '<li>' . $elem['name'] . '<a href="?del=' . $elem['id'] . '">' . 'удалить' . '</a>' . '</li>';
}

echo $res;
```

//4
```php
<meta charset="utf-8">
<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');

$del = $_GET['del'];
$query = "DELETE FROM users WHERE id=$del";
$res = mysqli_query($link, $query) or die(mysqli_error($link));

$query = "SELECT * FROM users";
$data = mysqli_query($link, $query) or die(mysqli_error($link));
$res = '<table>';
$res .= '<tr>';
$res .= '<th>' . 'id' . '</th>';
$res .= '<th>' . 'name' . '</th>';
$res .= '<th>' . 'age' . '</th>';
$res .= '<th>' . 'salary' . '</th>';
$res .= '<th>' . 'delete' . '</th>';
$res.= '</tr>';
foreach ($data as $elem) {
    $res .= '<tr>';
    $res .= '<td>' . $elem['id'] . '</td>';
    $res .= '<td>' . $elem['name'] . '</td>';
    $res .= '<td>' . $elem['age'] . '</td>';
    $res .= '<td>' . $elem['salary'] . '</td>';
    $res .= '<td>' . '<a href="?del=' . $elem['id'] . '">' . 'удалить' . '</a>' . '</td>';
    $res.= '</tr>';
}

$res .= '</table>';

echo $res;
```

### Просмотр данных из БД 
//2
```php
<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');

$query = "SELECT * FROM users";
$data = mysqli_query($link, $query) or die(mysqli_error($link));
$res = '';

foreach ($data as $elem) {
    $res .=  '<a href="show.php?id=' . $elem['id'] . '">' . $elem['name'] . '</a><br>';
}

echo $res;



<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');

$show = $_GET['id'];
$query = "SELECT * FROM users WHERE id = $show";
$data = mysqli_query($link, $query) or die(mysqli_error($link));
$res = '';

foreach ($data as $elem) {
    $res .= '<div>';
    $res .= '<p>' . 'имя: ' . '<span class="name">' . $elem['name'] . '</span>' . '</p>';
    $res .= '<p>' . 'возраст: ' . '<span class="age">' . $elem['age'] . '</span><br>';
    $res .= 'зарплата: ' . '<span class="salary">' . $elem['salary'] . '</span>' . '</p><br>';
    $res .= '</div>';
    $res .= '</a>';
}

echo $res;
```

### Добавление новой записи в БД
//1 и 2
```php
<form action="" method="POST">
    <input name="name" value="<?php if (isset($_POST['name'])) echo $_POST['name'] ?>">
    <input name="age" value="<?php if (isset($_POST['age'])) echo $_POST['age'] ?>">
    <input name="salary" value="<?php if (isset($_POST['salary'])) echo $_POST['salary'] ?>">
    <input type="submit">
</form>

<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');

if (!empty($_POST)) {
    $name = $_POST['name'];
    $age = $_POST['age'];
    $salary = $_POST['salary'];
    $query = "INSERT INTO users SET name='$name', age='$age', salary='$salary'";
    mysqli_query($link, $query) or die(mysqli_error($link));
}
```

### Редактирование записи в БД
//1, 2, 3 и 4
```php
<meta charset="utf-8">
<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');

$id = $_GET['id'];
$query = "SELECT * FROM users WHERE id=$id";
$result = mysqli_query($link, $query) or die(mysqli_error($link));
$user = mysqli_fetch_assoc($result);
?>

<form action="save.php" method="POST">
    <input type="hidden" name="id" value="<?= $id ?>">
    <input name="name" value="<?= $user['name'] ?>">
    <input name="age" value="<?= $user['age'] ?>">
    <input name="salary" value="<?= $user['salary'] ?>">
    <input type="submit">
</form>



<meta charset="utf-8">
<?php
/**
 * @var $link mysqli
 */

require_once('connect.php');
$id = $_POST['id'];
$name = $_POST['name'];
$age = $_POST['age'];
$salary = $_POST['salary'];

$query = "UPDATE users SET name='$name', age='$age', salary='$salary' WHERE id=$id";
$result = mysqli_query($link, $query) or die(mysqli_error($link));

echo 'юзер успешно изменен!';
```